# sdkwork-api-shop-standalone-gateway Specs

Component root: `crates/sdkwork-api-shop-standalone-gateway`.

The component contract declares the thin standalone host boundary. The host consumes the complete
Shop owner assembly contribution, binds Web Framework once, and owns the single HTTP listener.
