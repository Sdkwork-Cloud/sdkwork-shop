# SDKWork Shop App SDK Specs

This directory contains the local component contract for `sdkwork-shop-app-sdk`.

Machine authority:

- `component.spec.json`
- `../sdk-manifest.json`

The SDK family uses `sdk-manifest.json` as the single per-family metadata source. Retired per-family `sdk-manifest.json` files must not be restored.
